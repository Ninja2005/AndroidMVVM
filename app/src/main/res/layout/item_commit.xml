<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
                name="commit"
                type="com.hqumath.androidmvvm.entity.CommitEntity"/>

        <variable
                name="callback"
                type="com.hqumath.androidmvvm.adapters.CommitListAdapter.ClickCallback"/>
    </data>

    <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:orientation="horizontal"
            app:cardUseCompatPadding="true"
            android:onClick="@{() ->  callback.onItemClick(commit)}">

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="10dp">

            <TextView
                    android:id="@+id/tv_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    android:textSize="14sp"
                    tools:text="user"
                    android:text="@{commit.commit.committer.name}"/>


            <TextView
                    android:id="@+id/tv_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:maxLines="2"
                    android:textSize="14sp"
                    tools:text="提交记录"
                    android:text="@{commit.commit.message}"/>

            <TextView
                    android:id="@+id/tv_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="12sp"
                    tools:text="2019-01-01"
                    android:text="@{commit.commit.committer.date}"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
